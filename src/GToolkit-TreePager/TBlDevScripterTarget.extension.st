Extension { #name : #TBlDevScripterTarget }

{ #category : #'*GToolkit-TreePager' }
TBlDevScripterTarget >> onPaneModel: aPaneModel [
	self
		// GtTreePagerFlowPanesElement;
		// GtTreePagerPaneElement;
		@ [ :each | each paneModel = aPaneModel ]
]
